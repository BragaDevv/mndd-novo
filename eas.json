{
  "cli": {
    "version": ">= 3.0.0"
  },
  "build": {
    "production": {
      "android": {
        "buildType": "app-bundle"
      },
      "env": {
        "GOOGLE_SERVICES_JSON": "${GOOGLE_SERVICES_JSON}",
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      }
    }
  }
}
