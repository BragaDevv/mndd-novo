{
  "cli": {
    "version": ">= 3.0.0"
  },
  "build": {
    "production": {
      "android": {
        "buildType": "apk",
        "workflow": "managed"
      },
      "env": {
        "GOOGLE_SERVICES_JSON": "${GOOGLE_SERVICES_JSON}"
      }
    }
  }
}
