{
  "cli": {
    "version": ">= 3.0.0"
  },
  "build": {
    "production": {
      "android": {
        "buildType": "app-bundle"
      },
      "env": {
        "GOOGLE_SERVICES_JSON": "${GOOGLE_SERVICES_JSON}"
      }
    },
    "apk": {
      "android": {
        "buildType": "apk"
      },
      "env": {
        "GOOGLE_SERVICES_JSON": "${GOOGLE_SERVICES_JSON}"
      }
    }
  }
}
